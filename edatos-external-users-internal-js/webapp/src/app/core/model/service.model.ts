/**
 * Services available for external users. It is not part of any entity, but it is necessary in order to filter the external users.
 */
export enum Service {
    FILTERS = 'FILTERS',
    FAVORITES = 'FAVORITES'
}
