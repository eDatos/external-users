export interface GenericConfig {
    endpoint: {
        appUrl: any;
    };
    metamac: {
        visualizerPath: string;
    };
    metadata: {
        defaultLanguage: string;
        availableLanguages: string[];
    };
}
